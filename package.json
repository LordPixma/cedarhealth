{
  "name": "cedarhealth",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "gen:seed": "node scripts/gen-seed.mjs",
    "db:local": "wrangler d1 execute cedarhealth --local --file=./migrations/0001_init.sql && node scripts/gen-seed.mjs && wrangler d1 execute cedarhealth --local --file=./migrations/0002_seed.sql",
    "db:remote": "wrangler d1 execute cedarhealth --remote --file=./migrations/0001_init.sql && node scripts/gen-seed.mjs && wrangler d1 execute cedarhealth --remote --file=./migrations/0002_seed.sql"
  },
  "devDependencies": {
    "wrangler": "^4.47.0"
  }
}
